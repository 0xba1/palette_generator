import 'package:flutter/material.dart';

class ColorChoicePage extends StatelessWidget {
  const ColorChoicePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
